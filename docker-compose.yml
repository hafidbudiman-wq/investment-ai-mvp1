services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: investai
    ports:
      - "5432:5432"
    volumes:
      - investai_pgdata:/var/lib/postgresql/data
volumes:
  investai_pgdata:
